# Lambda Function in Python: Addition of Two Numbers

## 🎯 Aim
To write a Python program that defines a **lambda function** which takes two arguments `a` and `b`, and returns their sum.

## 🧠 Algorithm
1. Get two integer inputs from the user.
2. Use a **lambda function** to define a function `f` that returns `a + b`.
3. Call the function with the user inputs and print the result.

## 🧾 Program
```
num1 = int(input("Enter the first number (a): "))
num2 = int(input("Enter the second number (b): "))
f = lambda a, b: a + b
result = f(num1, num2)
print("The sum of the two numbers is:", result)
```

## Output
<img width="416" height="209" alt="image" src="https://github.com/user-attachments/assets/69a3de47-2610-4792-a3bd-0bb40002695a" />


## Result
The Python program defining a anonymous lambda function to calculate and return the sum of two numbers was successfully implemented, tested, and verified.
